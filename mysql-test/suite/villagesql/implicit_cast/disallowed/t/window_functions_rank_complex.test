# Test window functions with COMPLEX (rank)

--source include/villagesql/not_implemented_complex_type.inc
--source include/villagesql/install_complex_extension.inc

CREATE TABLE t_complex (id INT, val COMPLEX);

INSERT INTO t_complex VALUES
  (1, '(1.0,2.0)'),
  (2, '(3.0,4.0)'),
  (3, '(5.0,6.0)');

SELECT RANK() OVER (PARTITION BY val ORDER BY id) FROM t_complex;

DROP TABLE t_complex;

--source include/villagesql/uninstall_complex_extension.inc
