# Test window functions with non-COMPLEX ordering

--source include/villagesql/install_complex_extension.inc

CREATE TABLE t_complex (id INT PRIMARY KEY, val COMPLEX);

INSERT INTO t_complex VALUES
  (1, '(1.0,2.0)'),
  (2, '(3.0,4.0)'),
  (3, '(5.0,6.0)');

SELECT id, val, ROW_NUMBER() OVER (ORDER BY id) as row_num
FROM t_complex
ORDER BY id;

DROP TABLE t_complex;

--source include/villagesql/uninstall_complex_extension.inc
